import crypto from 'node:crypto';
import path from 'node:path';
import {DatabaseSync} from './sqlite.js';
const DB_PATH=process.env.DB_PATH||'/app/data/data.sqlite';
const SECRET=process.env.JWT_SECRET||'ULGURJI_CHANGE_SECRET';
const GEOFENCE_M=Number(process.env.GEOFENCE_M||150);
const db=new DatabaseSync(DB_PATH);
db.exec(`CREATE TABLE IF NOT EXISTS employee_entries(id INTEGER PRIMARY KEY AUTOINCREMENT,employee_id INTEGER NOT NULL,consumer_id INTEGER NOT NULL,entry_date TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'DRAFT',contact_name TEXT DEFAULT '',order_amount TEXT DEFAULT '',payment_status TEXT DEFAULT '',notes TEXT DEFAULT '',next_action TEXT DEFAULT '',lat REAL,lon REAL,accuracy REAL,created_at TEXT NOT NULL,updated_at TEXT NOT NULL,submitted_at TEXT,fieldwork_data TEXT DEFAULT '{}')`);
try{db.exec(`ALTER TABLE employee_entries ADD COLUMN fieldwork_data TEXT DEFAULT '{}'`)}catch{}
db.exec(`CREATE TABLE IF NOT EXISTS employee_entry_files(id INTEGER PRIMARY KEY AUTOINCREMENT,entry_id INTEGER NOT NULL,filename TEXT NOT NULL,mime TEXT DEFAULT 'application/octet-stream',data BLOB NOT NULL,created_at TEXT NOT NULL)`);
const q=s=>db.prepare(s),now=()=>new Date().toISOString(),b64=s=>Buffer.from(s).toString('base64url');
function verify(t){const a=String(t||'').split('.');if(a.length!==3)throw Error('AUTH_INVALID');const sig=b64(crypto.createHmac('sha256',SECRET).update(a[0]+'.'+a[1]).digest());if(sig!==a[2])throw Error('AUTH_INVALID');const p=JSON.parse(Buffer.from(a[1],'base64url').toString());if(!p.exp||p.exp<Date.now()/1000)throw Error('AUTH_INVALID');return p}
function send(res,status,data){res.writeHead(status,{'Content-Type':'application/json','Cache-Control':'no-store','Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'Authorization, Content-Type','Access-Control-Allow-Methods':'GET, POST, OPTIONS'});res.end(JSON.stringify(data))}
async function json(req){return await new Promise((ok,no)=>{let a=[],n=0;req.on('data',c=>{n+=c.length;if(n>12e6){no(Error('BODY_TOO_LARGE'));req.destroy();return}a.push(c)});req.on('end',()=>{try{ok(JSON.parse(Buffer.concat(a).toString()||'{}'))}catch(e){no(Error('INVALID_JSON'))}});req.on('error',no)})}
function auth(req){const h=req.headers.authorization||'';if(!h.startsWith('Bearer '))throw Error('AUTH_INVALID');return verify(h.slice(7))}
function cleanFile(f){return {name:path.basename(String(f?.name||'photo')).slice(0,120),type:String(f?.type||'application/octet-stream').slice(0,120),data:String(f?.data||'')}}
function getEntry(id,u){const e=q('SELECT * FROM employee_entries WHERE id=?').get(id);if(!e||e.employee_id!==u.id)throw Error('ENTRY_NOT_FOUND');return e}
function safeFieldwork(v){try{const x=typeof v==='string'?JSON.parse(v||'{}'):v;return x&&typeof x==='object'&&!Array.isArray(x)?x:{}}catch{return {}}}
function assigned(e,c,d=now().slice(0,10)){return !!q('SELECT 1 FROM assignments WHERE employee_id=? AND consumer_id=? AND work_date=?').get(e,c,d)}
function employee(u){return q("SELECT id,region,district FROM users WHERE id=? AND role='employee' AND active=1").get(u.id)}
function consumer(id){return q('SELECT * FROM consumers WHERE id=? AND active=1').get(id)}
function territoryOk(u,c){const e=employee(u);if(!e||!e.region||!c.region)return true;const n=s=>String(s||'').trim().toLowerCase().replace(/[ʻ’‘`']/g,"'").replace(/[–—-]/g,' ').replace(/\s+/g,' ');return n(e.region)===n(c.region)}
function validateGps(b){const lat=Number(b.lat),lon=Number(b.lon),accuracy=Number(b.accuracy);if(!Number.isFinite(lat)||!Number.isFinite(lon)||!Number.isFinite(accuracy))throw Error('GPS_REQUIRED');if(lat<-90||lat>90||lon<-180||lon>180)throw Error('GPS_INVALID');if(accuracy<0)throw Error('GPS_ACCURACY_INVALID');if(accuracy>10)throw Error('GPS_ACCURACY_TOO_LOW');return {lat,lon,accuracy}}
function distanceM(a,b,c,d){const R=6371000,r=Math.PI/180,p1=+a*r,p2=+c*r,dp=(+c-+a)*r,dl=(+d-+b)*r,x=Math.sin(dp/2)**2+Math.cos(p1)*Math.cos(p2)*Math.sin(dl/2)**2;return 2*R*Math.asin(Math.sqrt(Math.min(1,x)))}
function enforceVisitGps(b,u,c){const gps=validateGps(b);if(!territoryOk(u,c))throw Error('TERRITORY_MISMATCH');if(c.lat==null||c.lon==null)throw Error('CONSUMER_LOCATION_NOT_APPROVED');const dist=distanceM(gps.lat,gps.lon,c.lat,c.lon);if(dist>GEOFENCE_M)throw Error('OUTSIDE_GEOFENCE');return {...gps,distance_m:dist}}
function requiredSubmission(fieldwork,files){const fw=safeFieldwork(fieldwork),roles=new Set((files||[]).map(f=>f.role).filter(Boolean));const has=r=>roles.has(r);if(!fw.outcome)throw Error('OUTCOME_REQUIRED');if(!has('seal')||!has('meter')||!has('reading')||!has('gas'))throw Error('REQUIRED_PHOTOS_MISSING');if(String(fw.company_active)==='0'&&(!has('network_removed')||!has('act')))throw Error('INACTIVE_COMPANY_FILES_REQUIRED')}
export async function handleEmployeeApi(req,res,url){
 if(!url.startsWith('/api/employee-')&&!url.startsWith('/api/manager-employee-'))return false;
 if(req.method==='OPTIONS'){send(res,204,{});return true}
 let u;try{u=auth(req)}catch(e){send(res,401,{error:'AUTH_INVALID'});return true}
 if(url.startsWith('/api/manager-employee-')){
  if(u.role!=='admin'){send(res,403,{error:'ADMIN_ONLY'});return true}
  try{
   if(req.method==='GET'&&url==='/api/manager-employee-entries'){
    const p=new URL(req.url,'http://x').searchParams,eid=+(p.get('employee_id')||0),cid=+(p.get('consumer_id')||0),date=String(p.get('date')||'').trim();let w='1=1',a=[];if(eid){w+=' AND e.employee_id=?';a.push(eid)}if(cid){w+=' AND e.consumer_id=?';a.push(cid)}if(date){w+=' AND e.entry_date=?';a.push(date)}const rows=q(`SELECT e.*,u.employee_name,c.name consumer_name,(SELECT COUNT(*) FROM employee_entry_files f WHERE f.entry_id=e.id) files_count FROM employee_entries e JOIN users u ON u.id=e.employee_id JOIN consumers c ON c.id=e.consumer_id WHERE ${w} ORDER BY e.id DESC LIMIT 500`).all(...a);for(const r of rows){r.fieldwork=safeFieldwork(r.fieldwork_data);r.files=q('SELECT id,filename,mime,created_at FROM employee_entry_files WHERE entry_id=? ORDER BY id').all(r.id)}return send(res,200,{items:rows}),true}
   if(req.method==='GET'&&url.startsWith('/api/manager-employee-entry-files/')){const id=+url.split('/').pop(),f=q('SELECT f.*,e.employee_id FROM employee_entry_files f JOIN employee_entries e ON e.id=f.entry_id WHERE f.id=?').get(id);if(!f)return send(res,404,{error:'FILE_NOT_FOUND'});res.writeHead(200,{'Content-Type':f.mime,'Content-Length':f.data.length,'Content-Disposition':`inline; filename="${String(f.filename).replace(/"/g,'')}"`,'Cache-Control':'no-store'});res.end(f.data);return true}
  }catch(e){send(res,400,{error:e.message||'MANAGER_ENTRY_ERROR'});return true}return false
 }
 if(u.role!=='employee'){send(res,403,{error:'EMPLOYEE_ONLY'});return true}
 try{
  if(req.method==='POST'&&url==='/api/employee-visit-finish'){
   const b=await json(req),cid=+b.consumer_id||0,c=consumer(cid);if(!cid||!c)return send(res,404,{error:'CONSUMER_NOT_FOUND'}),true;if(!assigned(u.id,cid)||!territoryOk(u,c))return send(res,403,{error:'NOT_ASSIGNED_TODAY'}),true;if(!b.client_event_id)return send(res,400,{error:'CLIENT_EVENT_REQUIRED'}),true;const gps=enforceVisitGps(b,u,c),open=q("SELECT * FROM visits WHERE employee_id=? AND consumer_id=? AND substr(started_server,1,10)=? AND status IN ('STARTED','IN_PROGRESS') ORDER BY id DESC LIMIT 1").get(u.id,cid,now().slice(0,10));if(!open)return send(res,409,{error:'VISIT_NOT_STARTED'}),true;const end=now();q('UPDATE visits SET ended_server=?,end_lat=?,end_lon=?,end_accuracy=?,distance_m=?,status=?,outcome=?,notes=? WHERE id=?').run(end,gps.lat,gps.lon,gps.accuracy,gps.distance_m,'FINISHED',String(b.outcome||''),String(b.notes||''),open.id);return send(res,200,{ok:true,visit_id:open.id,status:'FINISHED',distance_m:gps.distance_m}),true
  }
  if(req.method==='GET'&&url==='/api/employee-consumers'){
   const p=new URL(req.url,'http://x').searchParams,term=String(p.get('q')||'').trim(),limit=Math.min(500,Math.max(1,+(p.get('limit')||100))),page=Math.max(1,+(p.get('page')||1)),off=(page-1)*limit,date=now().slice(0,10);let w='c.active=1 AND a.employee_id=? AND a.work_date=?',a=[u.id,date];if(term){w+=' AND (c.name LIKE ? OR c.district LIKE ? OR c.branch LIKE ? OR c.region LIKE ? OR c.mahalla LIKE ? OR c.street LIKE ? OR c.phone LIKE ?)';a.push(...Array(7).fill('%'+term+'%'))}const from='consumers c JOIN assignments a ON a.consumer_id=c.id';const total=q(`SELECT COUNT(*) n FROM ${from} WHERE ${w}`).get(...a).n,items=q(`SELECT c.id,c.name,c.branch,c.district,c.mahalla,c.street,c.category,c.activity,c.phone,c.region,c.lat,c.lon FROM ${from} WHERE ${w} ORDER BY c.name LIMIT ? OFFSET ?`).all(...a,limit,off);return send(res,200,{items,total,page,limit,has_more:off+items.length<total}),true}
  if(req.method==='GET'&&url.startsWith('/api/employee-entries')){const p=new URL(req.url,'http://x').searchParams,cid=+(p.get('consumer_id')||0),rows=cid?q('SELECT * FROM employee_entries WHERE employee_id=? AND consumer_id=? ORDER BY id DESC').all(u.id,cid):q('SELECT * FROM employee_entries WHERE employee_id=? ORDER BY id DESC LIMIT 100').all(u.id);for(const r of rows)r.fieldwork=safeFieldwork(r.fieldwork_data);return send(res,200,{items:rows}),true}
  if(req.method==='POST'&&url==='/api/employee-entry'){
   const b=await json(req),cid=+b.consumer_id||0;if(!cid)return send(res,400,{error:'CONSUMER_REQUIRED'}),true;const c=consumer(cid);if(!c)return send(res,404,{error:'CONSUMER_NOT_FOUND'}),true;if(!assigned(u.id,c.id)||!territoryOk(u,c))return send(res,403,{error:'NOT_ASSIGNED_TODAY'}),true;const status=b.status==='SUBMITTED'?'SUBMITTED':'DRAFT';let gps=null;if(status==='SUBMITTED')gps=enforceVisitGps(b,u,c);else if(b.lat!=null||b.lon!=null||b.accuracy!=null)gps=validateGps(b);const ts=now(),id=+b.entry_id||0;let eid=id;const fieldwork=safeFieldwork(b.fieldwork_data||b.fieldwork||{}),fw=JSON.stringify(fieldwork),attachments=Array.isArray(b.attachments)?b.attachments.map(cleanFile):[];if(status==='SUBMITTED')requiredSubmission(fieldwork,b.attachments||[]);if(id){const old=getEntry(id,u);if(old.consumer_id!==c.id)throw Error('ENTRY_CONSUMER_MISMATCH');q('UPDATE employee_entries SET consumer_id=?,status=?,contact_name=?,order_amount=?,payment_status=?,notes=?,next_action=?,lat=?,lon=?,accuracy=?,updated_at=?,submitted_at=?,fieldwork_data=? WHERE id=?').run(cid,status,String(b.contact_name||''),String(b.order_amount||''),String(b.payment_status||''),String(b.notes||''),String(b.next_action||''),gps?.lat??null,gps?.lon??null,gps?.accuracy??null,ts,status==='SUBMITTED'?ts:old.submitted_at,fw,id)}else{eid=q('INSERT INTO employee_entries(employee_id,consumer_id,entry_date,status,contact_name,order_amount,payment_status,notes,next_action,lat,lon,accuracy,created_at,updated_at,submitted_at,fieldwork_data) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)').run(u.id,cid,ts.slice(0,10),status,String(b.contact_name||''),String(b.order_amount||''),String(b.payment_status||''),String(b.notes||''),String(b.next_action||''),gps?.lat??null,gps?.lon??null,gps?.accuracy??null,ts,ts,status==='SUBMITTED'?ts:null,fw).lastInsertRowid}if(status==='DRAFT')q('DELETE FROM employee_entry_files WHERE entry_id=?').run(eid);for(const f0 of attachments){const f=cleanFile(f0),raw=f.data.includes(',')?f.data.split(',').pop():f.data;if(raw)q('INSERT INTO employee_entry_files(entry_id,filename,mime,data,created_at) VALUES(?,?,?,?,?)').run(eid,f.name,f.type,Buffer.from(raw,'base64'),ts)}return send(res,200,{ok:true,entry_id:eid,status,consumer_id:cid,distance_m:gps?.distance_m??null,geofence_m:GEOFENCE_M}),true
  }
  if(req.method==='GET'&&url.startsWith('/api/employee-entry-files/')){const id=+url.split('/').pop(),f=q('SELECT f.*,e.employee_id FROM employee_entry_files f JOIN employee_entries e ON e.id=f.entry_id WHERE f.id=?').get(id);if(!f||f.employee_id!==u.id)return send(res,404,{error:'FILE_NOT_FOUND'});res.writeHead(200,{'Content-Type':f.mime,'Content-Length':f.data.length,'Content-Disposition':`inline; filename="${String(f.filename).replace(/"/g,'')}"`,'Cache-Control':'no-store'});res.end(f.data);return true}
 }catch(e){send(res,400,{error:e.message||'ENTRY_ERROR'});return true}return false
}
