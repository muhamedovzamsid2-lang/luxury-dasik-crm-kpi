(()=>{const HEAT_RE=/(бюджет\s*иссиқлик|бюджет.*иссиқлик.*манба|иссиқлик.*манба.*бюджет|бюджет.*тепло|тепло.*источник.*бюджет|budget.*heat|heat.*source.*budget)/i;const ZERO_KEYS=/^(0|0[.,]0+|0\s*%|—|-)$/;const text=v=>String(v??'').replace(/\s+/g,' ').trim();const isHeat=v=>HEAT_RE.test(text(v));const isZero=v=>ZERO_KEYS.test(text(v));const rowIsExcluded=row=>{const s=text(row?.innerText);if(isHeat(s))return true;const cells=[...row.querySelectorAll('td')].map(x=>text(x.innerText));if(cells.length<2)return false;const meaningful=cells.filter(v=>v&&!/^\d+$/.test(v));const numeric=cells.filter(v=>isZero(v)||/^[+-]?\d+(?:[.,]\d+)?\s*%?$/.test(v));return numeric.length>=Math.max(2,cells.length-1)&&cells.slice(1).every(v=>!v||isZero(v)||/^[+-]?0(?:[.,]0+)?\s*%?$/.test(v));};function clean(){document.querySelectorAll('tr').forEach(r=>{if(rowIsExcluded(r))r.remove()});document.querySelectorAll('.consumer,.visitor,.metric-card,.card').forEach(el=>{if(isHeat(el.innerText))el.remove()});document.querySelectorAll('[data-value],.kpi,.metric-value').forEach(el=>{if(isZero(el.innerText))el.closest('.metric-card,.card')?.remove()});document.querySelectorAll('.table-wrap').forEach(w=>{const rows=w.querySelectorAll('tbody tr');if(rows.length===0){w.closest('.card')?.remove()}})}const css=document.createElement('style');css.textContent='.card{padding:14px;margin-bottom:10px}.kpi-grid{gap:9px}.metric-card{min-height:92px}.metric-value{font-size:23px;margin-top:7px}.table-wrap th,.table-wrap td{padding:7px 9px;font-size:10px}.consumer{padding:10px;margin:6px 0}.section-title{margin-bottom:9px}.admin-heading{margin-bottom:10px}';document.head.appendChild(css);new MutationObserver(clean).observe(document.body,{childList:true,subtree:true});window.addEventListener('load',()=>setTimeout(clean,80));setTimeout(clean,300)})();