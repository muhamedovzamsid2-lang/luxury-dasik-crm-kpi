import http from 'node:http';
import fs from 'node:fs';
import path from 'node:path';
import crypto from 'node:crypto';
import { fileURLToPath } from 'node:url';
import { DatabaseSync } from 'node:sqlite';
const __dirname=path.dirname(fileURLToPath(import.meta.url)),PORT=Number(process.env.PORT||3000),SECRET=process.env.JWT_SECRET||'CHANGE_THIS_SECRET_IN_PRODUCTION',GEO=Number(process.env.GEOFENCE_M||150);
const db=new DatabaseSync(process.env.DB_PATH||path.join(__dirname,'data.sqlite'));db.exec(`PRAGMA journal_mode=WAL;CREATE TABLE IF NOT EXISTS users(id INTEGER PRIMARY KEY,login TEXT UNIQUE NOT NULL,password_hash TEXT NOT NULL,role TEXT NOT NULL,employee_name TEXT NOT NULL,active INTEGER NOT NULL DEFAULT 1);CREATE TABLE IF NOT EXISTS consumers(id INTEGER PRIMARY KEY,name TEXT,branch TEXT,district TEXT,mahalla TEXT,street TEXT,category TEXT,activity TEXT,phone TEXT,lat REAL,lon REAL,location_accuracy REAL,location_set_server TEXT,active INTEGER DEFAULT 1);CREATE TABLE IF NOT EXISTS assignments(id INTEGER PRIMARY KEY AUTOINCREMENT,employee_id INTEGER NOT NULL,consumer_id INTEGER NOT NULL,work_date TEXT NOT NULL,UNIQUE(employee_id,consumer_id,work_date));CREATE TABLE IF NOT EXISTS visits(id INTEGER PRIMARY KEY AUTOINCREMENT,employee_id INTEGER NOT NULL,consumer_id INTEGER NOT NULL,started_server TEXT NOT NULL,ended_server TEXT,start_lat REAL,start_lon REAL,start_accuracy REAL,end_lat REAL,end_lon REAL,end_accuracy REAL,distance_m REAL,status TEXT NOT NULL,client_event_id TEXT UNIQUE NOT NULL);CREATE TABLE IF NOT EXISTS gps_points(id INTEGER PRIMARY KEY AUTOINCREMENT,employee_id INTEGER NOT NULL,ts_server TEXT NOT NULL,lat REAL NOT NULL,lon REAL NOT NULL,accuracy REAL,speed REAL,client_event_id TEXT UNIQUE NOT NULL);CREATE TABLE IF NOT EXISTS audit(id INTEGER PRIMARY KEY AUTOINCREMENT,ts_server TEXT NOT NULL,actor_id INTEGER,action TEXT NOT NULL,payload TEXT NOT NULL,prev_hash TEXT,hash TEXT NOT NULL);`);
const q=s=>db.prepare(s),now=()=>new Date().toISOString(),sha=s=>crypto.createHash('sha256').update(s).digest('hex'),ph=p=>sha(String(p??'')),b64=s=>Buffer.from(s).toString('base64url'),ub=s=>Buffer.from(s,'base64url').toString();
function jwtSign(o){let h=b64(JSON.stringify({alg:'HS256',typ:'JWT'})),p=b64(JSON.stringify({...o,exp:Math.floor(Date.now()/1000)+43200})),sig=b64(crypto.createHmac('sha256',SECRET).update(h+'.'+p).digest());return h+'.'+p+'.'+sig}function jwtVerify(t){let a=t.split('.');if(a.length!==3)throw Error();let sig=b64(crypto.createHmac('sha256',SECRET).update(a[0]+'.'+a[1]).digest());if(!crypto.timingSafeEqual(Buffer.from(sig),Buffer.from(a[2])))throw Error();let p=JSON.parse(ub(a[1]));if(p.exp<Date.now()/1000)throw Error();return p}function audit(actor,action,payload){let prev=q('SELECT hash FROM audit ORDER BY id DESC LIMIT 1').get()?.hash||'GENESIS',ts=now(),body=JSON.stringify(payload),hash=sha([prev,ts,actor||'',action,body].join('|'));q('INSERT INTO audit(ts_server,actor_id,action,payload,prev_hash,hash) VALUES(?,?,?,?,?,?)').run(ts,actor||null,action,body,prev,hash)}function dist(a,b,c,d){let R=6371000,r=Math.PI/180,p1=a*r,p2=c*r,dp=(c-a)*r,dl=(d-b)*r,x=Math.sin(dp/2)**2+Math.cos(p1)*Math.cos(p2)*Math.sin(dl/2)**2;return 2*R*Math.asin(Math.sqrt(Math.min(1,x)))}const coord=(a,b)=>Number.isFinite(+a)&&Number.isFinite(+b)&&+a>=-90&&+a<=90&&+b>=-180&&+b<=180,dateOk=x=>/^\d{4}-\d{2}-\d{2}$/.test(String(x||''));
q("INSERT OR IGNORE INTO users(id,login,password_hash,role,employee_name) VALUES(1,'admin',?,'admin','Раҳбар')").run(ph(process.env.ADMIN_PASSWORD||'change-me-admin'));q("INSERT OR IGNORE INTO users(id,login,password_hash,role,employee_name) VALUES(2,'employee1',?,'employee','Ходим 1')").run(ph(process.env.EMPLOYEE_PASSWORD||'change-me'));
async function body(req){return await new Promise((resolve,reject)=>{let x='';req.on('data',c=>{x+=c;if(x.length>2e6)reject(Error('BODY_TOO_LARGE'))});req.on('end',()=>{try{resolve(x?JSON.parse(x):{})}catch{reject(Error('BAD_JSON'))}})})}function send(res,code,data,type='application/json'){res.writeHead(code,{'Content-Type':type,'Cache-Control':'no-store','Access-Control-Allow-Origin':'*'});res.end(type.includes('json')?JSON.stringify(data):data)}function auth(req,res,admin=false){try{let h=req.headers.authorization||'';if(!h.startsWith('Bearer '))throw Error();let u=jwtVerify(h.slice(7));if(admin&&u.role!=='admin')throw Error('ADMIN');return u}catch(e){send(res,e.message==='ADMIN'?403:401,{error:e.message==='ADMIN'?'ADMIN_ONLY':'AUTH_INVALID'});return null}}
async function api(req,res,url){try{if(req.method==='GET'&&url==='/api/health')return send(res,200,{ok:true,time:now(),db:'sqlite',db_path:process.env.DB_PATH||path.join(__dirname,'data.sqlite'),geofence_m:GEO});if(req.method==='GET'&&url==='/api/metrics'){let u=auth(req,res,true);if(!u)return;let consumers=q('SELECT COUNT(*) n FROM consumers WHERE active=1').get().n,located=q('SELECT COUNT(*) n FROM consumers WHERE active=1 AND lat IS NOT NULL AND lon IS NOT NULL').get().n;return send(res,200,{consumers,located,unlocated:consumers-located,employees:q("SELECT COUNT(*) n FROM users WHERE role='employee' AND active=1").get().n,assignments:q('SELECT COUNT(*) n FROM assignments').get().n,visits:q('SELECT COUNT(*) n FROM visits').get().n,gps_points:q('SELECT COUNT(*) n FROM gps_points').get().n,audit:q('SELECT COUNT(*) n FROM audit').get().n,integrity:q('PRAGMA integrity_check').get().integrity_check,geofence_m:GEO})}if(req.method==='POST'&&url==='/api/login'){let b=await body(req),u=q('SELECT * FROM users WHERE login=? AND active=1').get(b.login);if(!u||ph(b.password)!==u.password_hash)return send(res,401,{error:'LOGIN_FAILED'});let token=jwtSign({id:u.id,role:u.role,name:u.employee_name});audit(u.id,'LOGIN',{login:u.login});return send(res,200,{token,user:{id:u.id,role:u.role,name:u.employee_name}})}if(req.method==='GET'&&url==='/api/me'){let u=auth(req,res);if(!u)return;return send(res,200,u)}if(req.method==='GET'&&url==='/api/employees'){let u=auth(req,res,true);if(!u)return;return send(res,200,q("SELECT id,employee_name,login,active FROM users WHERE role='employee' ORDER BY employee_name").all())}if(req.method==='GET'&&url.startsWith('/api/consumers')){let u=auth(req,res);if(!u)return;let p=new URL(req.url,'http://x').searchParams,x=p.get('q')||'',sql=x?"SELECT * FROM consumers WHERE active=1 AND (name LIKE ? OR district LIKE ? OR branch LIKE ? OR mahalla LIKE ?) ORDER BY name LIMIT 200":"SELECT * FROM consumers WHERE active=1 ORDER BY name LIMIT 200";return send(res,200,x?q(sql).all(...[x,x,x,x].map(v=>`%${v}%`)):q(sql).all())}if(req.method==='POST'&&url==='/api/consumers/location'){let u=auth(req,res,true);if(!u)return;let b=await body(req),c=q('SELECT id FROM consumers WHERE id=? AND active=1').get(b.consumer_id);if(!c||!coord(b.lat,b.lon))return send(res,400,{error:'LOCATION_INVALID'});let ts=now();q('UPDATE consumers SET lat=?,lon=?,location_accuracy=?,location_set_server=? WHERE id=?').run(+b.lat,+b.lon,b.accuracy??null,ts,b.consumer_id);audit(u.id,'CONSUMER_LOCATION',b);return send(res,200,{ok:true,server_time:ts})}if(req.method==='POST'&&url==='/api/assign'){let u=auth(req,res,true);if(!u)return;let b=await body(req);if(!dateOk(b.work_date))return send(res,400,{error:'ASSIGNMENT_INVALID'});let e=q("SELECT id FROM users WHERE id=? AND role='employee' AND active=1").get(b.employee_id),c=q('SELECT id FROM consumers WHERE id=? AND active=1').get(b.consumer_id);if(!e||!c)return send(res,404,{error:'EMPLOYEE_OR_CONSUMER_NOT_FOUND'});q('INSERT OR IGNORE INTO assignments(employee_id,consumer_id,work_date) VALUES(?,?,?)').run(b.employee_id,b.consumer_id,b.work_date);audit(u.id,'ASSIGN',b);return send(res,200,{ok:true})}if(req.method==='GET'&&url==='/api/today'){let u=auth(req,res);if(!u)return;let p=new URL(req.url,'http://x').searchParams,d=dateOk(p.get('date'))?p.get('date'):now().slice(0,10),eid=u.role==='admin'?Number(p.get('employee_id')||2):u.id,rows=q(`SELECT c.*,a.id assignment_id,v.id visit_id,v.status visit_status,v.started_server,v.ended_server,v.distance_m FROM assignments a JOIN consumers c ON c.id=a.consumer_id LEFT JOIN visits v ON v.id=(SELECT vv.id FROM visits vv WHERE vv.consumer_id=a.consumer_id AND vv.employee_id=a.employee_id AND vv.started_server LIKE ? ORDER BY vv.id DESC LIMIT 1) WHERE a.employee_id=? AND a.work_date=? ORDER BY c.name`).all(`${d}%`,eid,d);return send(res,200,{date:d,employee_id:eid,items:rows})}if(req.method==='POST'&&url==='/api/gps'){let u=auth(req,res);if(!u)return;let b=await body(req);if(!coord(b.lat,b.lon)||!b.client_event_id)return send(res,400,{error:'GPS_INVALID'});let ts=now();q('INSERT OR IGNORE INTO gps_points(employee_id,ts_server,lat,lon,accuracy,speed,client_event_id) VALUES(?,?,?,?,?,?,?)').run(u.id,ts,+b.lat,+b.lon,b.accuracy??null,b.speed??null,String(b.client_event_id));audit(u.id,'GPS',b);return send(res,200,{ok:true,server_time:ts})}if(req.method==='POST'&&url==='/api/visit/start'){let u=auth(req,res);if(!u)return;let b=await body(req),c=q('SELECT * FROM consumers WHERE id=? AND active=1').get(b.consumer_id);if(!c)return send(res,404,{error:'CONSUMER_NOT_FOUND'});if(!coord(b.lat,b.lon)||!b.client_event_id)return send(res,400,{error:'LOCATION_REQUIRED'});let dte=now().slice(0,10),a=q('SELECT id FROM assignments WHERE employee_id=? AND consumer_id=? AND work_date=?').get(u.id,b.consumer_id,dte);if(!a)return send(res,403,{error:'NOT_ASSIGNED_TODAY'});let open=q('SELECT id FROM visits WHERE employee_id=? AND consumer_id=? AND ended_server IS NULL').get(u.id,b.consumer_id);if(open)return send(res,409,{error:'VISIT_ALREADY_OPEN',visit_id:open.id});let d=coord(c.lat,c.lon)?dist(+b.lat,+b.lon,+c.lat,+c.lon):null,status=d===null?'NO_GEOFENCE':d<=GEO?'VALID':'OUTSIDE_GEOFENCE',ts=now(),r=q('INSERT INTO visits(employee_id,consumer_id,started_server,start_lat,start_lon,start_accuracy,distance_m,status,client_event_id) VALUES(?,?,?,?,?,?,?,?,?)').run(u.id,b.consumer_id,ts,+b.lat,+b.lon,b.accuracy??null,d,status,String(b.client_event_id));audit(u.id,'VISIT_START',{...b,distance_m:d,status});return send(res,200,{ok:true,visit_id:Number(r.lastInsertRowid),server_time:ts,distance_m:d,status})}if(req.method==='POST'&&url==='/api/visit/end'){let u=auth(req,res);if(!u)return;let b=await body(req),v=q('SELECT * FROM visits WHERE id=? AND employee_id=?').get(b.visit_id,u.id);if(!v)return send(res,404,{error:'VISIT_NOT_FOUND'});if(v.ended_server)return send(res,409,{error:'VISIT_ALREADY_CLOSED'});let ts=now();q('UPDATE visits SET ended_server=?,end_lat=?,end_lon=?,end_accuracy=? WHERE id=?').run(ts,coord(b.lat,b.lon)?+b.lat:null,coord(b.lat,b.lon)?+b.lon:null,b.accuracy??null,b.visit_id);audit(u.id,'VISIT_END',b);return send(res,200,{ok:true,server_time:ts})}if(req.method==='GET'&&url==='/api/live'){let u=auth(req,res,true);if(!u)return;let since=new Date(Date.now()-900000).toISOString();return send(res,200,{server_time:now(),rows:q(`SELECT u.id,u.employee_name,g.ts_server,g.lat,g.lon,g.accuracy,g.speed FROM users u JOIN (SELECT employee_id,MAX(id) max_id FROM gps_points WHERE ts_server>=? GROUP BY employee_id)x ON x.employee_id=u.id JOIN gps_points g ON g.id=x.max_id WHERE u.role='employee' AND u.active=1`).all(since)})}if(req.method==='GET'&&url==='/api/report'){let u=auth(req,res,true);if(!u)return;let p=new URL(req.url,'http://x').searchParams,d=dateOk(p.get('date'))?p.get('date'):now().slice(0,10),es=q("SELECT id,employee_name FROM users WHERE role='employee' AND active=1").all();return send(res,200,{date:d,result:es.map(e=>{let planned=q('SELECT COUNT(*) n FROM assignments WHERE employee_id=? AND work_date=?').get(e.id,d).n,visited=q('SELECT COUNT(*) n FROM visits WHERE employee_id=? AND started_server LIKE ?').get(e.id,`${d}%`).n,valid=q("SELECT COUNT(*) n FROM visits WHERE employee_id=? AND started_server LIKE ? AND status='VALID'").get(e.id,`${d}%`).n,suspicious=q("SELECT COUNT(*) n FROM visits WHERE employee_id=? AND started_server LIKE ? AND status<>'VALID'").get(e.id,`${d}%`).n;return {employee_id:e.id,employee:e.employee_name,planned,visited,not_visited:Math.max(0,planned-visited),valid,suspicious,completion:planned?Math.round(visited/planned*100):0}})})}if(req.method==='GET'&&url==='/api/audit'){let u=auth(req,res,true);if(!u)return;let rows=q('SELECT * FROM audit ORDER BY id DESC LIMIT 500'),all=rows.all(),prev='GENESIS',valid=true;for(const r of [...all].reverse()){let ex=sha([prev,r.ts_server,r.actor_id||'',r.action,r.payload].join('|'));if(ex!==r.hash||r.prev_hash!==prev){valid=false;break}prev=r.hash}return send(res,200,{chain_valid:valid,rows:all})}return false}catch(e){console.error(e);return send(res,500,{error:e.message||'SERVER_ERROR'})}}
const server=http.createServer(async(req,res)=>{if(req.method==='OPTIONS'){res.writeHead(204,{'Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'Authorization,Content-Type'});return res.end()}let url=new URL(req.url,'http://localhost').pathname;if(url.startsWith('/api/')){let done=await api(req,res,url);if(done!==false)return}let file=url==='/'?'/index.html':url;if(file.includes('..'))return send(res,400,{error:'BAD_PATH'});let p=path.join(__dirname,'public',file);if(fs.existsSync(p)&&fs.statSync(p).isFile()){let ext=path.extname(p),types={'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.json':'application/json'};res.writeHead(200,{'Content-Type':types[ext]||'application/octet-stream'});return fs.createReadStream(p).pipe(res)}send(res,404,{error:'NOT_FOUND'})});server.listen(PORT,()=>console.log(`ULGURJI ONLINE NAZORAT :${PORT}`));
